document.addEventListener("DOMContentLoaded", function () {
    console.log("✅ Schedule Admin JS Loaded!");

    // Basic functionality for Schedule admin
    // This is separate from the ticket_admin.js to avoid errors
}); 